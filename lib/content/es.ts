import type { Content } from './types';

/**
 * Spanish translation — intentionally empty for now.
 *
 * Fill in any subset of `Content`; whatever is missing falls back to English,
 * so a partial translation degrades gracefully instead of breaking the build.
 */
export const es: Partial<Content> = {};
